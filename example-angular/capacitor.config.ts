import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.payu.upipluginsampleapp',
  appName: 'example-angular',
  webDir: 'www'
};

export default config;
